import React from "react";

export default function Search({onClick}) {
  return (
    <>
        <div>Also React</div>
        <button onClick={onClick}>
            Update counter
        </button>
    </>
  );
}
